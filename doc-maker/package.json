{
  "name": "doc-maker",
  "version": "0.1.0",
  "description": "the document maker of the docloop project",
  "type": "module",
  "main": "./pkg/doc_maker.js",
  "types": "./pkg/doc_maker.d.ts",
  "files": [
    "pkg/**/*"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "build:release": "wasm-pack build --target web --release --out-dir pkg",
    "clean": "rm -rf pkg target"
  },
  "devDependencies": {},
  "keywords": ["wasm", "pdf", "document"],
  "authors": ["in-th3-l00p"],
  "license": "MIT"
} 